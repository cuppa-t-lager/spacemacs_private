# -*- mode: snippet -*-
# name: arr.map((item, i, arr) => {...})
# key: es6 =>
# group: es6
# --
`(unless (eq (char-before) ?.) ".")`map((${1:item}, ${2:i}, ${3:arr}) => `(if (> (%lines %) 1) (concat "{ " (!%!) " }") %)`$0)`(if (eolp) ";")`